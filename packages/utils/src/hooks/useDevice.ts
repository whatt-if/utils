/**
 * Calculates the area of a rectangle.
 *
 * @param {number} width - The width of the rectangle.
 * @param {number} height - The height of the rectangle.
 * @returns {number} The area of the rectangle.
 *
 * @example
 * // Example usage:
 * const sample = useDevice();
 * console.log(sample);
 */
const useDevice = () => ({});

export { useDevice };
