name: '❤️‍🔥 CI @whatt-if/utils'

on:
  pull_request:
    branches:
      - main

jobs:
  run-checks:
    uses: whatt-if/actions/.github/workflows/checks.yml@main

  publish:
    uses: whatt-if/actions/.github/workflows/publish.yml@main
